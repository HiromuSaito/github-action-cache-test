name: cache-test

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  cache-test:
    name: CACHE Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: echo
        run: echo "This is a test for caching"
